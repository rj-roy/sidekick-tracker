// import { ApiError } from "../../utils/ApiError.js";
// import { rExRepository } from "../registeredExtesion/r-ex.repository.js"

// export const userService = {
//     async verifyExtId (extensionId: string) {
//         const isExist = await rExRepository.isExistExtId(extensionId);
//         if(!isExist) {
//             throw new ApiError(404, "Extension isn't registered yet!");
//         };

//         const isValid = ""
//     },
// }